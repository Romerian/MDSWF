# MDSWF

## Projects

- [Test Application](./test-application) — a simple web app with Start and
  Reset controls for showing and hiding “Hello World”.

To run the app locally:

```bash
cd test-application
npm install
npm run dev
```
